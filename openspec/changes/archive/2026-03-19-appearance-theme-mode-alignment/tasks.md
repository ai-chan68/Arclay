## 1. Theme Model

- [x] 1.1 Replace the shared UI theme state model with `light`, `dark`, and `system`.
- [x] 1.2 Resolve `system` mode into a live effective theme using OS preference.
- [x] 1.3 Persist the selected appearance mode and apply the resolved theme to document-level attributes.

## 2. Settings Experience

- [x] 2.1 Replace the old named theme cards in the appearance tab with `Light`, `Dark`, and `System`.
- [x] 2.2 Update appearance copy and iconography so the settings page reads like a standard desktop appearance control.

## 3. Token Alignment

- [x] 3.1 Map EasyWork's shared UI tokens into a light theme suitable for Codex-style appearance semantics.
- [x] 3.2 Add a dark-theme token set for the same shared UI token layer.
- [x] 3.3 Verify key workspace surfaces and settings surfaces render coherently under the new light/dark theme model.
